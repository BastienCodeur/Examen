{% extends 'base.html.twig' %}

{% block title %}Rubriques{% endblock %}

{% block body %}
    <h1>Rubriques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rubrique.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ rubrique.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rubriques_index') }}">back to list</a>

    <a href="{{ path('rubriques_edit', {'id': rubrique.id}) }}">edit</a>

    {{ include('rubriques/_delete_form.html.twig') }}
{% endblock %}
